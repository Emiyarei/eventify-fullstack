# Eventify Fullstack (merged)

## Run locally
```bash
npm install
cd client && npm install && cd ..
npm run dev
```
- Server: http://localhost:5100
- Client: http://localhost:3000

API
- GET /api/events
- POST /api/events
- PUT /api/events/:id
- DELETE /api/events/:id
